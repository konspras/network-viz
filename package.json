{
  "name": "network-viz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run prepare-public-data --silent && node scripts/generate-manifest.cjs",
    "dev": "vite",
    "prebuild": "npm run prepare-public-data --silent && node scripts/generate-manifest.cjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:manifest": "node scripts/generate-manifest.cjs",
    "prepare-public-data": "tsx scripts/prepare-public-data.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "pixi.js": "^8.14.0"
  }
}
